    //define functions in js
    let log=function(message) {
        console.log(message);
    }

    //in ts we can use arrow function
    let doLog=(message)=>{
        console.log(message);
    }

    // or more simple as follows
    let Dolog=(message)=> console.log(message);
    